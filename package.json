{
  "name": "alice-proxy",
  "version": "0.1.0",
  "description": "alice skill to help testing other skills",
  "repository": "github.com/exromany/alice-proxy",
  "author": "exromany",
  "license": "MIT",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "deploy:alias": "now alias",
    "deploy:now": "now && now alias",
    "deploy": "lerna run build && yarn deploy:now"
  },
  "devDependencies": {
    "@types/node": "^10.12.18",
    "typescript": "^3.2.2"
  }
}
